# Contracts: Period Filter

This feature does not introduce new external API contracts.
The `Dataset` entity schema in `docs/entities/dataset.md` and the `dataset` crate define the data model.
The filtering logic is internal to the Engine Worker.
